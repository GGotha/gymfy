mutation Authenticate($password: String!, $email: String!) {
  authenticate(password: $password, email: $email) {
    user {
      id
      name
      email
      role {
        id
        name
        active
        created_at
        updated_at
        __typename
      }
      created_at
      updated_at
      __typename
    }
    token
  }
}
